[pytest]
# Workspace-level suite: the shared utilities only. Each project owns its own
# pytest.ini and is run from its own directory (see projects/*/README.md).
testpaths = shared/tests
addopts = -q --strict-markers -r fEX
